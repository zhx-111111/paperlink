# 📜 PaperLink

> 写一封信，等一个人。寄信 / 实时镜像两种模式的手写信笺应用。
> 完整方案见 [SPEC.md](./SPEC.md)。

技术栈：**Cloudflare Workers + KV + Durable Object**，纯静态前端（无构建步骤）。
手写引擎、溶解动画、毛玻璃 UI 嫁接自 [tom-riddles-diary](https://github.com/zhx-111111/tom-riddles-diary)（Riddle 网页版）。

---

## ✨ 功能一览

| 模块 | 说明 |
| --- | --- |
| 两种通讯模式 | ✉️ **寄信**（默认）：停笔→发送，对端打开信页逐笔同速重放，支持**暂停 / 重播**；🌊 **实时镜像**（实验功能）：落笔即见，逐笔/逐点流同步 |
| 强制信纸同步 | 对方切换主题/横竖屏/屏幕比例，本端信纸即时跟随（含未解锁彩蛋纸） |
| 对话大厅 | 书架式卡片（封面=信纸缩略），9 位邀请码创建/加入，每账户最多 5 个对话，重命名/删除 |
| 未读限制 | 对方未查看完前**最多可发 3 页**（可调 `pending_page_limit`），已读回执实时放行 |
| 在线状态 | 绿点"在线" / 红点"离线"，多端互斥（新设备登录踢旧设备） |
| 账户面板 | 昵称/头像（6 选 1）本地化存储，兑换码彩蛋，退出/注销/销毁日记本 |
| 彩蛋体系 | E1 星夜 / E2 樱花 / E3 玫瑰金墨水 / E4 金箔图标 / E5 头像框 / E6 墨迹渐隐 / **E7 畅寄五十页（未读完也能寄，上限 50 页）** / **RT 实时镜像（兑换码开启）**；全部支持管理页「公开/兑换码」二选一 |
| 信纸模板 | 管理页自定义信纸：选色器选信纸/笔迹颜色；可选粘贴或上传作用于 .page-paper 的 CSS（动态信纸、渐变笔迹等），服务端校验作用域与注入 |
| 音乐播放 | 实验功能：书写房内搜网易云歌曲并播放（转发 Meting-API，管理页可关） |
| 管理后台 | 实时在线人数、应用参数（含**开放注册开关**、实时镜像总开关）、兑换码批量生成/CSV、模板管理、房间诊断与休眠清理 |
| Apple 风 UI | 毛玻璃材质、弹簧动画、iOS 开关/卡片/底部抽屉，自动适配浅色与深色模式 |

---

## 🚀 部署（Cloudflare Workers）

> 本项目只走一条部署链路：**Cloudflare 官方的 Workers Builds（控制台 Git 集成）**——推送即自动构建部署。
> 原有的 GitHub Actions 工作流已移除，不要再同时配置第二条链路，避免互相覆盖。

**所有运行变量已在 `wrangler.jsonc` 中预设**，开箱即部署。唯一需要动手的是 KV 存储库——请先读下面这条重要机制，避免"绑定了却显示未绑定"。

> ⚠️ **KV 绑定必读**：每次部署都会用配置文件里的绑定**整体覆盖**线上绑定。
> 只在控制台手动绑定、却不写进 `wrangler.jsonc`，**下一次推送/重新部署就会被清掉**（这正是"绑定了却显示未绑定"的根因）。
> 正确做法：在控制台创建 KV 命名空间 → **把它的 ID 粘贴进 `wrangler.jsonc` 的 `kv_namespaces`** → 提交推送。此后绑定随配置永久生效。

### 粘贴 KV ID（推荐，最简单、绑定永久生效）

1. 先部署一次（此时无 KV，站点提示"未绑定"，属正常）；
2. 控制台 **Storage & Databases → KV → Create namespace**（建议名 `paperlink-kv`），复制命名空间 **ID**；
3. 打开 `wrangler.jsonc`，把 `kv_namespaces` 里注释行的 `<把你的KV命名空间ID粘贴到这里>` 换成该 ID 并取消注释（绑定名 `PAPERLINK_KV` 勿改）；
4. 提交并推送 → 绑定随配置生效且**永久保留**，无需在控制台再手动绑。

### 收尾

- **设置 Secrets**（Worker → Settings → Variables and Secrets）：
  - `PL_JWT_SECRET`：会话签名密钥（**强烈建议**，不设则用内置默认值）；
  - `ADMIN_PASSWORD`：管理页密码（默认 `paperlink2026`，**务必修改**）；
  - `SECRET_TURNSTILE`：可选，Cloudflare Turnstile 服务端密钥；不配置则注册免人机验证。
  - 已开启 `keep_vars: true`，控制台里设好的 Secrets / 变量不会被后续部署覆盖。
- （可选）Custom Domains 绑定自己的域名。

### 备选：wrangler CLI（本地手动部署）

```bash
git clone https://github.com/zhx-111111/paperlink.git
cd paperlink
npm install
npx wrangler login
npx wrangler deploy        # 首次部署（DO migrations 自动执行）
```

同样建议先把 KV ID 填入 `wrangler.jsonc` 再部署，理由同上。

### 排查"绑定了却显示未绑定"

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 控制台绑过，站点仍提示未绑定 | 绑定只在控制台、未写入配置，被后续部署覆盖 | 把 KV ID 写进 `wrangler.jsonc` 重新推送部署 |
| 绑定名写成了别的 | 绑定名必须是 `PAPERLINK_KV` | 改名或重建绑定 |
| 部署到了 Pages 而非 Workers | 本项目为 Workers（assets + DO） | 用 Workers 方式部署 |

### Vars（已预设，可在控制台修改）

`default_theme` / `idle_timeout_ms` / `keep_pages` / `dormant_after_hour` / `page_ttl_days` / `archive_after_pages` / `max_pts_per_page` / `cursor_sync_interval_ms` / `pressure_min_width` / `pressure_max_width` / `turnstile_site_key`（可选，前端 Turnstile widget key）——全部 snake_case，见下表。

---

## 🔧 参数（管理页可调，覆盖 vars）

| 参数 | 默认 | 说明 |
| --- | --- | --- |
| `allow_register` | 开 | **是否开放注册**（关闭后 /join 拒收新用户） |
| `realtime_allowed` | 开 | 实时镜像总开关（实验功能，用户另需兑换码 RT） |
| `pending_page_limit` | 3 | 对方未读完前最多可发送的页数（1–10） |
| `keep_pages` | 10 | 每对话保留最近 N 页，超出 FIFO 遗忘（5–50） |
| `dormant_after_hour` | 24 | 房间无活跃超时（小时）→ 删信页；双倍超时彻底删房间 |
| `page_ttl_days` | 30 | 信页 KV TTL（天） |
| `max_pts_per_page` | 5000 | 单页笔迹点提示上限 |
| `cursor_sync_interval_ms` | 200 | 光标/逐点流同步节流 |
| `idle_timeout_ms` | 2500 | 新信横幅自动展开的空闲判定 |
| `pressure_min_width` | 0.6 | 压感最细笔迹（0.2–3） |
| `pressure_max_width` | 2.4 | 压感最粗笔迹（0.2–3） |
| `default_theme` | tom | 默认信纸 |

---

## 🗝 管理后台（/admin）

- **实时在线人数**：总在线 + 分房间明细，每 10 秒刷新（3 分钟无心跳的记录自动剔除）；
- **运行诊断**：KV/Secrets 状态、会话/房间/信页/模板/兑换码计数、房间列表、一键清理休眠房间；
- **兑换码**：按彩蛋批量生成（含实验功能 RT），CSV 导出；
- **信纸模板**：上传 `.css`（≤50KB，仅允许作用于 `.page-paper`，拒绝 `@import`/外链 `url()`/脚本注入）+ 可选背景图（≤500KB），启用后所有人主题栏可见。

---

## 💡 生产化设计（方案 → 上线之间解决的问题）

1. **KV 免费额度保护**：
   - 实时笔迹只走 WebSocket 广播，**不落 KV**；实时模式本身设为兑换码解锁的实验功能，控制使用面；
   - 房间活跃时间先写 DO storage，**5 分钟/断线时才回写 KV**（原方案 30s 一写，单房日写近 3000 次）；
   - 在线计数仅在人数变化或 60s 心跳时写入；昵称/头像/主题偏好等用户信息全部本地化（`localStorage`），KV 只存跨设备必需数据。
2. **注册开关**：`allow_register` 管理页参数，配合 Turnstile 双保险。
3. **限流与防刷**：注册（8 次/分/同 IP）、邀请码试错（20 次/分/同 IP）、提交（1.2s 冷却/用户）、WS 事件（60 事件/秒/连接丢弃）、提交载荷 ≤2.5MB。
4. **发送节流阀**：未读 3 页上限防止单方刷屏，同时压缩信页 KV 体积。
5. **休眠清理**：24h 无活跃删信页保元数据，48h 彻底删除，管理页可手动清扫。
6. **已知取舍**：WS token 走 URL query（浏览器 WebSocket 无法设自定义头），请启用 HTTPS；内存限流为单实例尽力而为。

---

## 🏗 结构

```
├── wrangler.jsonc          # Workers + assets + KV + DO(RoomDO) + migrations
├── src/
│   ├── index.js            # 路由：注册登录 / 房间 / 信件 / 兑换码 / 模板 / 管理
│   ├── roomdo.js           # Durable Object：WS 广播、在线计数、实时门槛校验
│   ├── config.js           # 参数、主题、彩蛋目录
│   └── util.js             # token(HMAC)、邀请/兑换码、校验、CSS 安全校验
└── public/
    ├── index.html          # 书写房（工具栏/主题栏/书信集/重放控制/发送栏）
    ├── join.html           # 注册 / 加入（Turnstile）
    ├── hall.html           # 对话大厅（书架）
    ├── me.html             # 账户面板（兑换码/销毁）
    ├── admin.html          # 管理后台
    ├── css/paperlink.css
    ├── js/                 # room / join / hall / me / admin / shared / inkpad
    ├── icons/              # SVG symbol + 180/192/512 PNG
    └── fonts/              # Dancing Script + 马善政楷体（中文手写）
```

---

## 📖 玩法

1. `/join` 注册（昵称 + 密码 + 头像），创建对话拿到 9 位邀请码，交给 TA；
2. TA 用邀请码加入，进入同一本日记；
3. **寄信模式**：写满一页点「发送」，墨迹被喝掉，信飞进对方书信集——TA 打开时笔迹由无到有逐笔浮现，可暂停/重播；
4. **实时镜像**（兑换码解锁）：⇄ 切换后落笔即见，横竖屏与信纸样式强制同步；
5. 对方未读完 3 页前不能再寄——等 TA 读；
6. 彩蛋用兑换码在「我的」页解锁。

---

## 🎨 自定义信纸模板

管理后台「信纸模板」支持**用户共创信纸**：

1. 在模板卡片点击 **下载信纸模板骨架**（或直接取 `public/templates/letter-template-skeleton.css`）；
2. 骨架已备好信纸与笔迹的逻辑骨架：底色/渐变、`var(--tpl-bg)` 背景图适配、纹理/横线/做旧伪元素、`var(--ink-color)` 呼应笔迹色——按注释填充自己的样式即可；
3. 管理页填模板名称 → 上传改好的 `.css`（≤50KB，须作用于 `.page-paper`）→ 可选背景图（≤500KB）与固定笔迹色 → 「上传并启用」；
4. 启用后所有用户的主题栏「更多」中即出现这张新信纸（等同一个已解锁彩蛋）。

服务端会校验：拒绝 `@import` / 外链 `url()` / 脚本注入，确保样式只作用于信纸容器。

---

## 🗒 更新记录

**v3.4**
- **修复双指手势会移动信纸本身**：此前第二根手指落下时会重置对端比例并重排信纸、iOS 捏合触发 visualViewport 变化又盲目重排，加上页面本身允许双指缩放，表现为信纸元素在手势中跳动移位。现在三管齐下——第二指落下不再触发重排与比例广播；visualViewport 仅在舞台布局盒真正变化时才重排；页面锁定双指缩放（`maximum-scale=1, user-scalable=no`），书写区整体 `touch-action: none`。双指从此只作用于纸内视口，信纸面积与位置保持固定；
- 全部历史需求逐项核对（首页布局 / 大厅书籍与返回 / 五十页畅寄 / 双指手势 / 兑换码复制 / 音乐 / 彩蛋公开与兑换码 / 管理页保存等），均已在代码中落实。

**v3.3**
- **双指手势重做（参考 Apple iOS）**：删掉双指橡皮，改为双指拖动移动纸面、捏合放大缩小（0.5x–3x，锚点跟手），手势打断的半截笔画仍会通知对端丢弃；书写/擦除/回放全部走视口变换，导出与同步仍用原始纸面坐标，互不干扰；翻页自动复位视口；
- **页面自适应**：书写房高度改用动态视口（100dvh），手机地址栏收缩不再吃掉纸面；窄屏（≤430px）隐藏站名、邀请码截断防溢出，横屏矮屏微调徽章与主题栏位置；
- **兑换码单击复制**：后台生成的兑换码码框单击即整体复制；
- **更多 canvas-ui 思路**：信件打开时落款一行做「解码」浮现动效（DecryptReveal 思路，尊重减少动态效果设置）。

**v3.2**
- **修复对话大厅不显示已加入的对话**：书架渲染依赖的主题注册表进大厅时没初始化，画第一本书就崩、整列空白；现在先加载注册表再渲染，并加了主题缺失时的兜底色；
- 已加入对话照旧显示为书形卡片：封面=信纸纹理、正面=对话名、右上角=未读页数角标；左上角「<」点击回首页；
- **新彩蛋 E7「畅寄五十页」**：兑换（或管理页公开）后，对方还没读完也能继续寄信，最多同时压 50 页未读信（默认 3 页）；上限由服务端按账号即时生效（3 秒轮询同步）。

**v3.1**（细节打磨 · 借鉴 [canvas-ui](https://github.com/DavidHDev/canvas-ui)）
- **纸面微反馈层**：落笔处泛起手绘感墨波，重压会溅出细小墨点；对端落笔/光标偶尔点出极轻的呼吸涟漪——用纯 Canvas 2D 重写 canvas-ui 的 Ripple / Droplets 思路，空闲自动停帧、尊重系统「减少动态效果」设置；
- 其余保持 v3.0。

**v3.0**（体验与同步大修）
- **交互修复**：清空整页不再弹确认框、点即清空；工具按钮列改为可上下滑动，矮屏不再挡住「我的」入口与发送栏；撤销图标重绘；大厅左上角新增返回键；
- **书写同步**：修复多指/多笔场景下进行中笔画被静默丢弃的问题；单条消息上限放宽到 900KB、整页提交笔数上限翻倍，长写不再整批丢笔；实时镜像逐点流改平滑曲线续画并按双方笔宽比例折算，两端笔迹一致；实时镜像可正常关闭；
- **双指橡皮**（riddle 式）：双指按住纸面直接擦，打断的半截笔画会通知对端同步丢弃；
- **等待横幅**：5 秒后自动缩小为头像框+在线状态，固定在「我的」头像下方，点一点复制邀请码；
- **在线状态**：WS 在场判定不再被轮询误降级，服务端叠加 60s 心跳计数双保险；
- **溶解动画升级**：发送/翻页时墨迹先轻化再化作颗粒升腾淡出，向 riddle 看齐；粒子数自适应封顶，低端机不掉帧；
- **音乐（实验）**：书写房内可搜网易云歌曲播放（转发 Meting-API，`music_allowed` 可关）；
- **图标七连点**：连点应用图标 7 次唤起隐藏浮窗，内容管理页可编辑（`secret_html`）；
- **彩蛋公开制**：所有彩蛋可勾选公开（全员可用）或走兑换码；兑换码改 cloud-mail 式——一码可多选彩蛋/信纸、可自定义可用次数，CSV 导出同步新字段；
- **自定义信纸重做**：删除图片上传，仅两种模式——选色器（信纸色+笔迹色）与 CSS 模板（粘贴或上传，可实现动态信纸/渐变笔迹）；所有选色一律取色器；
- **布局**：非全屏信纸长宽比跟随设备屏幕比例；主题按钮自动固定屏幕左上角（竖排）；
- **文案**：移除「读完才能继续」类提示与全部「喝掉/喝墨」表述。

**v2.0**（大改版）
- **首页**：按布局图重做——顶部图标+站名、对话大厅入口、我的入口；中部单书写板体验区（仿 riddle，含橡皮/撤销/清空，写一个大「?」唤起指南）；提示行 `write a big "?" to start.`；页脚支持管理页编辑 HTML、可无限向下延伸；
- **账号体系（cloud-mail 式）**：昵称+密码注册/登录，无需注册码；后台可查看全部用户、重置密码、删除账号；绑定 D1 后用户表走 D1（照搬 cloud-mail 模式），未绑定自动回退 KV；
- **KV 手动绑定可用**：CI 部署在未配 KV ID 时会自动读取线上已有绑定（含控制台手动绑的 KV/D1）并保留，不再整体覆盖清除；
- **信纸体系**：内置仅保留午夜墨/羊皮纸/素笺（午夜墨笔迹纯白）；管理页可勾选「信纸公开」，未公开信纸不显示、但可生成兑换码发放；主题栏改为 riddle 式同心圆（外环信纸色、内心笔迹色），只显示拥有的；
- **压感双参数**：`pressure_min_width` / `pressure_max_width`（均 0.2–3），压感公式随两参数自适应；
- **实时镜像**：未兑换 RT 时模式按钮整体不显示；
- **体验优化**：信件重放改为全屏/按对方比例最大化播放；去掉每页笔数与各类横幅标语；新增横屏按钮（方向锁+CSS 旋转双兜底）；全屏信纸铺满屏幕；新开一页镜像到对端；橡皮长按弹出滑条调大小；
- **状态修正**：3 秒全局轮询同步在线状态/待读计数/未读角标，修复「对方在线显示离线」「对方已读仍显示未读」；
- **管理页**：可上下滑动（原 overflow:hidden 锁死）、登录密码输入框重做并删除 CF 环境变量提示文案、新增修改管理密码/微信域名验证文件上传/首页内容编辑/用户管理；
- **图标**：应用图标改绿蓝渐变（SVG+180/192/512 PNG 全换）；
- **多端兼容**：全屏 API 全前缀+CSS 兜底、方向锁降级、iOS 视口 dvh、橡皮/触摸 touch-action 修正。

**v1.2**（问题修复版）
- **修复对端擦除半径换算错误**：接收端漏除 VW 基准，半径放大 1000 倍，对方轻轻一擦本端整页被清空；
- **修复擦除事件回环**：对端镜像的擦除会再次上报给对方，两端互相回发形成事件风暴；
- **修复大厅 ⋯ 卡片菜单无法打开**：全局点击监听在菜单打开的同一帧把它关掉；
- **修复在线列表误判离线**：`online/{code}` 时间戳只在人数变化时写，人数稳定的房间 3 分钟后被管理页"陈旧记录"规则剔除；
- **降低 DO 写入开销**：活跃时间改为内存记账 + 30s 节流写 DO storage（原每条 WS 消息写一次）；
- **管理页防爆破**：同 IP 连续 5 次密码错误锁 5 分钟（补齐 SPEC §9 承诺）；
- **安全加固**：邀请码/兑换码改用 `crypto.getRandomValues`（原 Math.random 可预测）；管理令牌改恒定时间比较；模板墨色服务端白名单校验（防存储型 XSS）；WS 单条消息 256KB 上限；
- **修复注册页 Turnstile 一次性令牌问题**：提交失败后自动重置验证组件，第二次提交不再必然失败；
- **修复未读上限提示**：被拒收时前端读不到服务端返回的真实待读数（apiJson 错误对象未携带响应体）；
- 其他：卡片菜单位置越界钳制、登出跳转竞争等小修。

**v1.1**
- **修复 KV 绑定丢失**：wrangler 部署会整体覆盖绑定，控制台手动绑定会在下次部署被清掉；改为「粘贴 KV ID 进配置」与「GitHub Actions 注入」两种持久方案，并新增 `keep_vars: true` 保护控制台 Secrets/变量；
- **注册开关**：新增管理页参数 `allow_register`，可随时关闭注册；
- **实时镜像实验化**：改为兑换码 `RT` 解锁 + 总开关 `realtime_allowed`，降低服务端额度压力；
- **Apple 风 UI 增强**：毛玻璃材质、弹簧动画、iOS 开关、底部抽屉式弹层、深浅色自适应；
- **字体瘦身**：移除 5.8MB 自托管楷体，改用系统楷体栈（Apple Kaiti SC 原生），加载更快；
- **信纸模板骨架**：提供可下载的模板骨架文件，填充样式即可上传新信纸；
- 修复：横屏首帧信纸方向、接收方误覆盖发送计数、房间人数状态、兑换码 CSV 键名偏移等。

**v1.0** —— 首个可部署版本。

---

*"I open at the close."* —— 本项目以 MIT 协议发布。
